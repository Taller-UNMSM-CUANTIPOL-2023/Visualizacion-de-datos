knitr::include_graphics("./images/logoPUCP.png")  #puedes centrar y aumentar o disminuir el tamaño del gráfico (buscar códigos en internet) #sirve para plotear cualquier imagen
knitr::include_graphics("./images/diccionario.png")
#getwd()
#setwd("")
library(rio)
data = import("./basedf/COVID Isolation.xlsx")
View(data)
knitr::include_graphics("./images/tlc.jpeg")
knitr::opts_chunk$set(warning = FALSE, message = FALSE)
library(rio) # Convocamos el paquete
data = import("welfare states data.xlsx")
knitr::include_graphics("id_paises.png")
class(data) # identificando el tipo de objeto
str(data) # Cómo están medidas las variables
nrow(data) # numero de filas
ncol(data) # numero de columnas
dim(data) # dimensión de mi data(un resumen de la # de filas y columnas)
# tamaño de data
names(data) #nombres de variables
#colnames(data) #alternativa de nombre de variables
#head(data) #6 primeros casos de mi data
#tail(data) #6 últimos casos de mi data
#attributes(data) #atributos de mi data
str(data) # Cómo están medidas las variables
str(data) # Cómo están medidas las variables
class(data) # identificando el tipo de objeto
nrow(data) # numero de filas
ncol(data) # numero de columnas
dim(data) # dimensión de mi data(un resumen de la # de filas y columnas)
# caso 1: idn
class(data$idn) # pj. idn figura como num, cuando en realidad es nominal politómica
data$idn = factor(data$idn) # lo convertiremos a factor
class(data$idn)
